# React Native/Expo 툴체인(Metro, autolinking, prebuild 로 생성되는 네이티브 프로젝트)은
# 중첩·심볼릭 링크된 node_modules 를 완전히 다루지 못합니다. 평탄한 트리를 쓰도록 고정합니다.
node-linker=hoisted

# Expo SDK 패키지들은 peer 범위를 느슨하게 선언하는 경우가 있어, 설치를 실패시키지 않고 경고만 남깁니다.
strict-peer-dependencies=false
