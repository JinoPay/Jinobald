/// <reference types="expo/types" />

// Expo 는 `expo start` 시 루트에 expo-env.d.ts 를 생성하지만 그 파일은 gitignore 대상입니다.
// 갓 클론한 저장소에서도 `pnpm typecheck` 가 통과하도록 같은 참조를 추적 파일로 둡니다.
// (레퍼런스 지시문은 중복되어도 무해합니다.)
