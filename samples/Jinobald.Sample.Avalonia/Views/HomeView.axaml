<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Jinobald.Sample.Avalonia.Views.HomeView">

    <StackPanel Spacing="16" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="🎉 Welcome to Jinobald!"
                   FontSize="32"
                   FontWeight="Bold"
                   HorizontalAlignment="Center" />

        <TextBlock Text="Cross-Platform MVVM Framework"
                   FontSize="18"
                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,24" />

        <TextBlock Text="✨ Features:" FontSize="16" FontWeight="SemiBold" />

        <StackPanel Spacing="8" Margin="32,0,0,0">
            <TextBlock Text="• DI Container (Prism 스타일)" />
            <TextBlock Text="• Navigation Service (비동기, 히스토리)" />
            <TextBlock Text="• Event Aggregator (Thread-safe)" />
            <TextBlock Text="• Dialog Service (In-window overlay)" />
            <TextBlock Text="• Theme Service (동적 테마 변경)" />
            <TextBlock Text="• Settings Service (자동 저장/로드)" />
            <TextBlock Text="• Splash Screen (필수)" />
        </StackPanel>

        <TextBlock Text="왼쪽 메뉴에서 데모를 선택하세요."
                   FontSize="14"
                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,24,0,0" />
    </StackPanel>
</UserControl>
