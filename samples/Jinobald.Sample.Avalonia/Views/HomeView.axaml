<UserControl
    x:Class="Jinobald.Sample.Avalonia.Views.HomeView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="16">
        <TextBlock
            HorizontalAlignment="Center"
            FontSize="32"
            FontWeight="Bold"
            Text="🎉 Welcome to Jinobald!" />

        <TextBlock
            Margin="0,0,0,24"
            HorizontalAlignment="Center"
            FontSize="18"
            Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
            Text="Cross-Platform MVVM Framework" />

        <TextBlock
            FontSize="16"
            FontWeight="SemiBold"
            Text="✨ Features:" />

        <StackPanel Margin="32,0,0,0" Spacing="8">
            <TextBlock Text="• DI Container (Prism 스타일)" />
            <TextBlock Text="• Navigation Service (비동기, 히스토리)" />
            <TextBlock Text="• Event Aggregator (Thread-safe)" />
            <TextBlock Text="• Dialog Service (In-window overlay)" />
            <TextBlock Text="• Theme Service (동적 테마 변경)" />
            <TextBlock Text="• Settings Service (자동 저장/로드)" />
            <TextBlock Text="• Splash Screen (필수)" />
        </StackPanel>

        <TextBlock
            Margin="0,24,0,0"
            HorizontalAlignment="Center"
            FontSize="14"
            Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
            Text="왼쪽 메뉴에서 데모를 선택하세요." />
    </StackPanel>
</UserControl>
