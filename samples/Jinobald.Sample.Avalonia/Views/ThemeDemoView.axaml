<UserControl
    x:Class="Jinobald.Sample.Avalonia.Views.ThemeDemoView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Margin="16" Spacing="16">
            <TextBlock
                FontSize="24"
                FontWeight="Bold"
                Text="🎨 Theme Demo" />

            <TextBlock Text="Dynamic theme switching with auto-save to settings." TextWrapping="Wrap" />

            <!--  테마 선택 섹션  -->
            <Border
                Margin="0,8,0,0"
                Padding="16"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1"
                CornerRadius="4">
                <StackPanel Spacing="12">
                    <TextBlock
                        FontSize="18"
                        FontWeight="SemiBold"
                        Text="🌓 Theme Selection" />

                    <TextBlock
                        Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                        Text="애플리케이션의 테마를 변경할 수 있습니다."
                        TextWrapping="Wrap" />

                    <!--  테마 버튼들  -->
                    <WrapPanel>
                        <Button
                            MinWidth="140"
                            Margin="0,0,8,8"
                            Command="{Binding SetLightThemeCommand}"
                            Content="☀️ Light Theme" />

                        <Button
                            MinWidth="140"
                            Margin="0,0,8,8"
                            Command="{Binding SetDarkThemeCommand}"
                            Content="🌙 Dark Theme" />

                        <Button
                            MinWidth="140"
                            Margin="0,0,8,8"
                            Command="{Binding SetSystemThemeCommand}"
                            Content="💻 System Theme" />
                    </WrapPanel>

                    <!--  현재 테마 표시  -->
                    <Border
                        Margin="0,8,0,0"
                        Padding="12"
                        Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                        BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                        BorderThickness="1"
                        CornerRadius="4">
                        <StackPanel Spacing="4">
                            <TextBlock
                                FontSize="12"
                                FontWeight="SemiBold"
                                Text="현재 테마:" />
                            <TextBlock
                                FontSize="16"
                                FontWeight="Bold"
                                Text="{Binding CurrentThemeName}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!--  테마 프리뷰 섹션  -->
            <Border
                Margin="0,8,0,0"
                Padding="16"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1"
                CornerRadius="4">
                <StackPanel Spacing="12">
                    <TextBlock
                        FontSize="18"
                        FontWeight="SemiBold"
                        Text="🎨 Theme Preview" />

                    <TextBlock
                        Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                        Text="다양한 UI 요소들이 현재 테마에 따라 어떻게 보이는지 확인할 수 있습니다."
                        TextWrapping="Wrap" />

                    <!--  샘플 UI 요소들  -->
                    <StackPanel Spacing="8">
                        <TextBlock Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}" Text="텍스트 요소" />

                        <Button Content="Primary Button" />

                        <TextBox Text="TextBox 예제" Watermark="입력하세요..." />

                        <CheckBox Content="CheckBox 예제" IsChecked="True" />

                        <RadioButton Content="RadioButton 예제" IsChecked="True" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!--  기능 설명 섹션  -->
            <Border
                Margin="0,8,0,0"
                Padding="16"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1"
                CornerRadius="4">
                <StackPanel Spacing="8">
                    <TextBlock
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="✅ Features" />

                    <StackPanel Margin="12,0,0,0" Spacing="4">
                        <TextBlock Text="• Light/Dark/System 테마 지원" />
                        <TextBlock Text="• 설정 자동 저장 (SettingsService 통합)" />
                        <TextBlock Text="• 커스텀 테마 등록 가능" />
                        <TextBlock Text="• GetThemeColor / GetThemeResource API" />
                        <TextBlock Text="• ThemeChanged 이벤트 지원" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!--  사용 예제  -->
            <Border
                Margin="0,8,0,0"
                Padding="16"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1"
                CornerRadius="4">
                <StackPanel Spacing="8">
                    <TextBlock
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="📝 Usage Example" />

                    <TextBlock
                        xml:space="preserve"
                        FontFamily="Consolas, Menlo, Monaco, monospace"
                        FontSize="11"
                        TextWrapping="Wrap">// 테마 변경
_themeService.SetTheme("Dark");

// 현재 테마 확인
var current = _themeService.CurrentTheme;

// 테마 변경 이벤트 구독
_themeService.ThemeChanged += OnThemeChanged;</TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
