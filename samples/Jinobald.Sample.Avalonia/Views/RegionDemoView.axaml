<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:jino="https://github.com/JinoPay/Jinobald"
             x:Class="Jinobald.Sample.Avalonia.Views.RegionDemoView">

    <ScrollViewer>
        <StackPanel Spacing="16" Margin="16">
            <TextBlock Text="📦 Region Demo"
                       FontSize="24"
                       FontWeight="Bold" />

            <TextBlock Text="Region 시스템을 사용하여 동적으로 뷰를 추가/제거할 수 있습니다."
                       TextWrapping="Wrap" />

            <!-- 컨트롤 섹션 -->
            <Border Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="16"
                    Margin="0,8,0,0">
                <StackPanel Spacing="12">
                    <TextBlock Text="🎮 Region Controls"
                               FontSize="18"
                               FontWeight="SemiBold" />

                    <TextBlock Text="버튼을 클릭하여 ContentRegion에 뷰를 추가하거나 제거할 수 있습니다."
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />

                    <!-- 버튼들 -->
                    <WrapPanel>
                        <Button Content="🔴 Add Red View"
                                Command="{Binding AddRedViewCommand}"
                                Margin="0,0,8,8"
                                MinWidth="140" />

                        <Button Content="🔵 Add Blue View"
                                Command="{Binding AddBlueViewCommand}"
                                Margin="0,0,8,8"
                                MinWidth="140" />

                        <Button Content="🟢 Add Green View"
                                Command="{Binding AddGreenViewCommand}"
                                Margin="0,0,8,8"
                                MinWidth="140" />

                        <Button Content="🗑️ Clear All"
                                Command="{Binding ClearRegionCommand}"
                                Margin="0,0,8,8"
                                MinWidth="140" />
                    </WrapPanel>

                    <!-- 결과 표시 -->
                    <Border Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="12"
                            Margin="0,8,0,0">
                        <StackPanel Spacing="4">
                            <TextBlock Text="마지막 액션:"
                                       FontWeight="SemiBold"
                                       FontSize="12" />
                            <TextBlock Text="{Binding LastAction}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Region 영역 -->
            <Border Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="16"
                    Margin="0,8,0,0">
                <StackPanel Spacing="12">
                    <TextBlock Text="📋 Content Region (ItemsControl)"
                               FontSize="18"
                               FontWeight="SemiBold" />

                    <TextBlock Text="ItemsControl로 구현된 Region입니다. 여러 뷰를 동시에 표시할 수 있습니다."
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />

                    <!-- ItemsControl Region -->
                    <ItemsControl jino:Region.Name="ContentRegion"
                                  MinHeight="200">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- 기능 설명 섹션 -->
            <Border Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="16"
                    Margin="0,8,0,0">
                <StackPanel Spacing="8">
                    <TextBlock Text="✅ Features"
                               FontSize="16"
                               FontWeight="SemiBold" />

                    <StackPanel Spacing="4" Margin="12,0,0,0">
                        <TextBlock Text="• XAML에서 RegionName 어태치드 프로퍼티로 Region 정의" />
                        <TextBlock Text="• ItemsControl, ContentControl 자동 어댑터 지원" />
                        <TextBlock Text="• RegionManager를 통한 동적 뷰 추가/제거" />
                        <TextBlock Text="• ViewModel 기반 뷰 생성 (View-ViewModel 자동 매핑)" />
                        <TextBlock Text="• Region별 뷰 활성화/비활성화 지원" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 사용 예제 -->
            <Border Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="16"
                    Margin="0,8,0,0">
                <StackPanel Spacing="8">
                    <TextBlock Text="📝 Usage Example"
                               FontSize="16"
                               FontWeight="SemiBold" />

                    <TextBlock TextWrapping="Wrap"
                               FontFamily="Consolas, Menlo, Monaco, monospace"
                               FontSize="11"
                               xml:space="preserve">// XAML에서 Region 정의
&lt;ItemsControl jino:Region.Name="ContentRegion" /&gt;

// 코드에서 View 추가 (View-first 패턴)
_regionManager.AddToRegion&lt;MyView&gt;("ContentRegion");

// 네비게이션 (Back/Forward 지원)
await _regionManager.NavigateAsync&lt;MyView&gt;("ContentRegion");

// Region 가져오기
var region = _regionManager.GetRegion("ContentRegion");</TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
