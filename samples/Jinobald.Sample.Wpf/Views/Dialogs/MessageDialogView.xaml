<UserControl
    x:Class="Jinobald.Sample.Wpf.Views.Dialogs.MessageDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Jinobald.Sample.Wpf.Views.Dialogs"
    MinWidth="400"
    MaxWidth="600">

    <UserControl.Resources>
        <local:MessageTypeToIconConverter x:Key="MessageTypeToIconConverter" />
    </UserControl.Resources>

    <Border
        Padding="24"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
        BorderThickness="1"
        CornerRadius="8">

        <StackPanel>
            <!--  제목  -->
            <StackPanel Orientation="Horizontal">
                <!--  아이콘  -->
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="24"
                    Text="{Binding MessageType, Converter={StaticResource MessageTypeToIconConverter}}" />

                <TextBlock
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="20"
                    FontWeight="SemiBold"
                    Text="{Binding Title}" />
            </StackPanel>

            <!--  메시지  -->
            <TextBlock
                Margin="0,16,0,0"
                MaxWidth="550"
                Text="{Binding Message}"
                TextWrapping="Wrap" />

            <!--  버튼  -->
            <StackPanel
                Margin="0,24,0,0"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    MinWidth="100"
                    Command="{Binding OkCommand}"
                    Content="확인" />
                <Button
                    MinWidth="100"
                    Margin="8,0,0,0"
                    Command="{Binding CancelCommand}"
                    Content="취소" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
